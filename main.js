
const quarterValue = (originalNumber) => {

        // new variable to hold the calculated result
    const newNumber = (originalNumber / 4)
        // return that result
        // RECALL that if a function returns a value, it MUST
        // be stored in a variable
    return newNumber
}

    // Invoke the function and store the return value
const answer = quarterValue(7340)

// Log the return value
console.log(answer)